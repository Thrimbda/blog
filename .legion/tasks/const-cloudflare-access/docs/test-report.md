# Test Report: const-cloudflare-access

## Summary

**Result**: PASS

The direct host shell does not provide `zola` or `terraform`, but this repository is a Nix environment. Final verification used `shell.nix` and the project command advertised by the shell hook.

## Site Build

- Command: `nix-shell --run "npx czon build --lang zh-Hans --lang en-US --lang ja-JP --lang es-ES --lang de-DE"`
- Result: **PASS**
- Evidence: build completed with `Build completed`, crawled/generated language outputs including `/const.html` and language variants, and produced 108 URLs.
- Notes: This is the correct project-level build path because `czon` generates the multilingual source/output needed before the final static site crawl.

### Baseline / Non-blocking Checks

- Command: `zola build`
- Result: **BLOCKED**
- Evidence: direct host shell returned `command not found: zola`.

- Command: `nix-shell --run "zola build"`
- Result: **FAIL, non-blocking for this task**
- Evidence: `get_url` failed resolving multilingual root content such as `@/_index.<lang>.md` when `czon` had not generated multilingual content first.
- Reason not blocking: `shell.nix` documents the project build command as `npx czon build --lang zh-Hans --lang en-US --lang ja-JP --lang es-ES --lang de-DE`, and that command passed.

## Terraform

- Command: `nix-shell -p terraform --run "terraform fmt -check -recursive infra/cloudflare-access"`
- Result: **PASS**
- Evidence: no output from fmt check after formatting.

- Command: `nix-shell -p terraform --run "cd infra/cloudflare-access && terraform init -backend=false"`
- Result: **PASS**
- Evidence: initialized Cloudflare provider `v5.19.1` and generated `.terraform.lock.hcl`.

- Command: `nix-shell -p terraform --run "cd infra/cloudflare-access && terraform validate"`
- Result: **PASS**
- Evidence: `Success! The configuration is valid.`

- Command: `terraform fmt -recursive && terraform init -backend=false && terraform validate`
- Result: **BLOCKED on direct host shell**
- Evidence: direct host shell returned `command not found: terraform`.

- No `terraform apply` was executed.

## Static Checks

- `content/const.md` exists and defines the `/const/` page.
- `config.toml` includes `nav_const` for `zh-Hans`, `en-US`, `ja-JP`, `es-ES`, and `de-DE`.
- `config.toml` appends `{ path = "@/const.md", label_key = "nav_const" }` to `extra.header_nav`.
- `.czon/src/*/content/const.md` exists for all configured languages after the project build.
- `.gitignore` ignores Terraform local state, plan, `.terraform/`, `terraform.tfvars`, and `*.auto.tfvars`, while preserving `.tfvars.example`.
- `infra/cloudflare-access/main.tf` destinations cover all required Access paths:
  - `${site_hostname}/const`
  - `${site_hostname}/const/`
  - `${site_hostname}/const/*`
- Tracked Terraform files contain placeholders/variables only; no obvious real Cloudflare API token, account id, zone id, team domain, email allowlist, IdP group, Access group, or service token was found.

## Artifacts

- `infra/cloudflare-access/.terraform.lock.hcl` was generated by `terraform init -backend=false` and should be included with Terraform configuration for provider reproducibility.
