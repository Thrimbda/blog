---
"title": "Zola Blog: Echtzeit-Editor und Backend-Lösungsdesign"
"summary": "Dieses Dokument analysiert detailliert den aktuellen Stand der Zola-Blog-Website, einschließlich Tech-Stack, Design-Sprache und Inhaltsmerkmalen, und entwirft eine Lösung zur Implementierung einer WYSIWYG-Markdown-Echtzeitbearbeitung. Kernempfehlung ist die Verwendung von Vditor als Editor und Supabase als Backend-Speicher- und Authentifizierungslösung. Es wird betont, dass keine Build-Chain eingeführt werden muss, um den bestehenden minimalistischen JS-Stil und die suCSS-Designsprache beizubehalten. Das Dokument erörtert auch wichtige Entscheidungspunkte wie den Bearbeitungsumfang und den Veröffentlichungs-Workflow und bietet alternative Optionen für Editor und Backend, um sicherzustellen, dass die Lösung zum bestehenden Projekt passt und einfach umzusetzen ist."
"tags":
  - "Zola"
  - "Markdown"
  - "WYSIWYG"
  - "Supabase"
  - "Echtzeitbearbeitung"
  - "Blog"
  - "Frontend"
  - "Design"
"date": "2026-01-06"
---

# Zola Blog: WYSIWYG Markdown Echtzeit-Editor + Supabase/GitHub Backend Lösungsdesign - Kontext

## Sitzungsfortschritt (2026-01-06)

### ✅ Abgeschlossen

-   Erste Bestandsaufnahme: Das Projekt ist eine Zola-Site, das Theme basiert auf anemone + suCSS, aktuell gibt es sehr wenig JS und CDN-Abhängigkeiten sind erlaubt.

### 🟡 In Bearbeitung

-   Erstellung eines detaillierten, überprüfbaren Designs (Editor-/Backend-Auswahl + Veröffentlichungs-/Synchronisations-Workflow + Implementierungsschritte).

### ⚠️ Blockiert/Ausstehend

-   Genaue Bedeutung von "Echtzeitbearbeitung" (Echtzeit-Vorschau vs. automatisches Speichern vs. Kollaboration).
-   Bearbeitungsumfang (nur wenige Seiten/Einzeldateien oder der gesamte `content/`-Bereich).
-   Veröffentlichungs-Workflow (nur Entwürfe speichern vs. Synchronisation mit `content/`, um Zola-Build auszulösen).

---

## Wichtige Dateien

-   `config.toml`: Site-Konfiguration und Navigation; Standard-Theme ist light, Mehrsprachigkeit wird unterstützt.
-   `templates/head.html`: Bindet global `static/css/reset.css`, `static/css/suCSS.css`, `static/css/style.css` und `static/js/script.js` ein, sowie über CDN `rxjs`, `highlight.js`, `mermaid`.
-   `static/css/suCSS.css`: Design-Tokens (CSS-Variablen) und Basis-Komponenten-Stile (Formulare, Buttons, Tabellen etc.); enthält Light/Dark-Theme-Variablen.
-   `static/css/style.css`: Site-spezifische benutzerdefinierte Styles (Navigation, Footer, Slider, Shortcode-Styles etc.).
-   `static/js/script.js`: Theme-Umschaltung (`data-theme` + localStorage) und Slider-Interaktion; kann für Admin-Seiten wiederverwendet werden, um einheitliche Benutzererfahrung zu gewährleisten.
-   `content/**`: Markdown-Inhaltsquellen; enthalten YAML-Frontmatter (`---`) und Shortcodes (z.B. `{{ slideshow(...) }}`).

---

## Wichtige Entscheidungen

| Entscheidung | Grund | Alternative | Datum |
|--------------|-------|-------------|-------|
| Standardempfehlung: "Reine JS-Admin-Seite + Supabase Speicher/Authentifizierung", Editor-Priorität: Vditor | Aktuelles Projekt hat keine Node/React-Build-Chain; bestehende CDN-Abhängigkeitskonvention; Vditor bietet WYSIWYG/IR/SV drei Modi und "Browser-basierter Markdown-Editor"-Positionierung, niedrigste Integrationskosten | React + shadcn + Milkdown/TipTap; oder GitHub Discussion/Issue als Backend | 2026-01-06 |

---

## Bestandsaufnahme-Zusammenfassung

### Tech-Stack/Deployment

-   Site ist Zola (`config.toml`), GitHub Actions verwendet `shalzz/zola-deploy-action` für Deployment auf `gh-pages` (`.github/workflows/*.yaml`).
-   Site tendiert zu "minimalistischem JS": Aktuell ist das einzige globale Skript `static/js/script.js`, der Rest ist CDN (`rxjs`, `highlight.js`, `mermaid`).

### Theme und Design-Sprache (muss beibehalten werden)

-   Visuelles Design basiert auf CSS-Variablen in `suCSS.css`: `--bg/--bg-light/--text/--text-light/--accent/--accent-light/--link/--border` etc.
-   Theme-Umschaltmechanismus: `document.documentElement[data-theme="light"|"dark"]`, synchronisiert mit `prefers-color-scheme` + localStorage (siehe `static/js/script.js`).
-   Komponentenstil: Buttons/Formulare/Tabellen/Listen-Marker etc. sind in `suCSS.css` definiert; neue UI sollte diese semantischen Elemente und Variablen priorisiert wiederverwenden, anstatt ein neues Design-System einzuführen.

### Inhaltsmerkmale (beeinflussen Editor)

-   Markdown kann enthalten:
    -   YAML-Frontmatter (Beispiel: `content/gcores-talks.md`)
    -   Zola-Shortcodes (Beispiel: `{{ slideshow(...) }}`, Templates in `templates/shortcodes/*`)
    -   Hervorgehobene Code-Blöcke, mermaid-Code-Blöcke
-   Risikopunkt: Ein WYSIWYG-Editor, der versucht, Shortcodes zu "verstehen/rendern", könnte den Originaltext umschreiben oder entfernen; benötigt wird ein Editor/Modus, der "unbekannte Syntax unverändert lässt", oder ein "Quelltext-Modus" im Editor als Fallback.

---

## Editor-Recherche (Online + Material)

> Ziel: Einbettbar, unterstützt WYSIWYG (Rich-Text/What-You-See-Is-What-You-Get) und gibt Markdown aus; idealerweise ohne Build-Chain (reines JS/CDN).

| Kandidat | Positionierung/Modi | Integrationsform | Theme/Anpassung | Wichtige Punkte | Fazit für dieses Projekt |
|---|---|---|---|---|---|
| **Vditor** | Browser-basierter Markdown-Editor; unterstützt WYSIWYG/IR(ähnlich Typora)/SV Split-View | Natives JS; offizielle Website und CDN-Form sind ausgereift (Seitentext betont explizit drei Modi) | Viele vorgefertigte Themes und Konfigurationsoptionen; kann über CSS-Variablen an suCSS angepasst werden | Unterstützt Echtzeitspeicherung, Drag & Drop Upload etc. (siehe Website-Beschreibung); MIT-Lizenz (`https://unpkg.com/vditor@3.10.4/LICENSE`) | **Erste Wahl**: Passt am besten zu "Keine Build-Chain + Echtzeitbearbeitung"; muss dringend überprüft werden, ob "Shortcodes/Frontmatter nicht zerstört werden" |
| **TOAST UI Editor** | Markdown/WYSIWYG Dual-Modus | CDN (`uicdn.toast.com/editor/latest/...`) oder npm | Hat Theme/Styling-System; kann über benutzerdefiniertes CSS angepasst werden | Dokumentation unterstützt explizit CDN und das Abschalten von `usageStatistics` (`https://unpkg.com/@toast-ui/editor@3.2.2/README.md`) | Alternative: Ausgereift, aber Styling-System ist "schwerer", Anpassung an suCSS-Theming könnte aufwändiger sein; Lizenz muss bei Integration noch geprüft werden |
| **Milkdown** | Plugin-basierter WYSIWYG Markdown-Editor (Typora-Stil), ProseMirror + remark | Benötigt normalerweise Bundler (React/Vue etc.) | Tiefe Anpassung möglich, aber hoher Engineering-Aufwand | README betont seine WYSIWYG-Markdown-Positionierung und MIT-Lizenz (`https://raw.githubusercontent.com/Milkdown/milkdown/main/README.md`) | Geeignet als Kandidat, wenn "React/shadcn eingeführt werden soll"; passt nicht zum aktuellen Zola-Projekt (benötigt Build-Chain) |
| **TipTap** | Rich-Text-Editor-Toolkit (eher HTML/Schema-orientiert) | React/Vue/Svelte etc. | Starke Anpassbarkeit, aber Markdown erfordert oft zusätzliche Konvertierung und Einschränkungen | Offizielle Seite betont "real time collaboration" etc., aber Markdown-first ist nicht der Kern | Nicht als erste Wahl empfohlen (Markdown- und Shortcode-Szenarien sind komplex) |

---

## Backend-Recherche (Ohne manuelles Backend)

| Lösung | Benötigt eigenen Service? | Authentifizierung/Berechtigungen | Speicher/Historie | Beziehung zu Zola-Veröffentlichung | Fazit |
|---|---|---|---|---|---|
| **Supabase** | Nein (gehostete DB/Auth/Storage) | Frontend verbindet direkt + RLS; anon key kann öffentlich sein | Zeilenbezogene Historie in DB muss selbst erstellt werden (z.B. revisions-Tabelle) | Benötigt "Synchronisation zum Repo/content", um statische Site zu beeinflussen | **Empfohlene Standardlösung**: Umsetzung am direktesten; klare Sicherheitsgrenzen (RLS); Bildspeicherung auch einfach |
| **GitHub Issue/Discussion** | Theoretisch kein Service nötig, aber in der Praxis oft OAuth-Token-Austausch-Service erforderlich | OAuth-Workflow komplex (reines Frontend kann Token-Austausch schwer sicher handhaben) | GitHub hat natürliche Historie/Revisionen | Synchronisation zur Site erfordert noch Skript, um Issue/Discussion in `content/` zu konvertieren | Alternative: Falls der Wunsch besteht, dass "Inhalte direkt GitHub-Daten sind", kann erwogen werden; aber Authentifizierung/Upload/Synchronisation ist komplexer |

Ergänzung: Der OAuth-Web-Flow von GitHub Docs erfordert im Schritt "Code gegen Access Token tauschen" explizit sowohl `client_id` als auch `client_secret`, was eine "reine Frontend-Direktverbindung zu GitHub als Schreib-Backend" sicherheitstechnisch sehr schwierig macht (es sei denn, man führt einen Token-Austausch-Service ein oder verwendet eingeschränkte Lösungen wie device flow).

---

## Schnelle Übergabe

**Hier beim nächsten Mal weitermachen:**

1.  In `plan.md` die Standardannahmen für "Echtzeitbearbeitung/Bearbeitungsumfang/Veröffentlichungs-Workflow" bestätigen und die Entscheidungspunkte festlegen, die von dir überprüft werden müssen.
2.  Basierend auf deinem Feedback, in den drei Lösungsansätzen (Supabase / GitHub Discussions / React-Ansatz) eine endgültige Empfehlung auswählen und die Implementierungsschritte festlegen.

**Zu beachten:**

-   Jede UI (einschließlich Admin) muss die CSS-Variablen und den Light/Dark-Mechanismus von `suCSS.css` wiederverwenden, ohne neue, den Stil brechende Theme-Systeme einzuführen.
-   Wichtig zu überprüfen: Ob der Editor Zola-Shortcodes und Frontmatter umschreibt.

---

*Zuletzt aktualisiert: 2026-01-06 20:30 von Codex*