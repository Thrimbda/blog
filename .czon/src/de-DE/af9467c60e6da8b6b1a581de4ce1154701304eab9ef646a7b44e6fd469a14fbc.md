---
"title": "Zola Blog: Echtzeitbearbeitung und Backend-Lösungsdesign"
"summary": "Dieses Dokument ist ein Designkonzept für die Ergänzung einer WYSIWYG-Echtzeitbearbeitungsfunktion für eine bestehende Zola-Static-Blog-Site. Das Dokument beginnt mit einer Ist-Analyse, die den Technologie-Stack der Site (Zola + GitHub Actions Deployment) und die beizubehaltende Designsprache (CSS-Variablen und Theme-Switching basierend auf suCSS.css) bestätigt. Der Kernabschnitt untersucht mehrere Markdown-Editoren (mit Schwerpunkt auf Vditor) und Backend-Lösungen ohne manuelle Programmierung (mit Schwerpunkt auf Supabase) und vergleicht deren Vor- und Nachteile sowie Kompatibilität. Das Dokument klärt auch offene Schlüsselfragen, wie die genaue Bedeutung von \"Echtzeitbearbeitung\", den Bearbeitungsumfang und den Veröffentlichungs-Workflow. Das Endziel ist die Gestaltung eines Bearbeitungs- und Veröffentlichungssystems, das keine komplexe Build-Kette einführt, den bestehenden Site-Stil beibehält und Zola-Shortcodes und Front Matter sicher verarbeiten kann."
"tags":
  - "Zola"
  - "WYSIWYG"
  - "Markdown-Editor"
  - "Supabase"
  - "Statischer Blog"
  - "Echtzeitbearbeitung"
  - "Vditor"
  - "Lösungsdesign"
"date": "2026-01-06"
---

# Zola Blog: WYSIWYG Markdown Echtzeitbearbeitung + Supabase/GitHub Backend-Lösungsdesign - Kontext

## Sitzungsfortschritt (2026-01-06)

### ✅ Abgeschlossen

-   Erste Ist-Analyse: Das Projekt ist eine Zola-Site, das Theme basiert auf anemone + suCSS, aktuell gibt es sehr wenig JS und CDN-Abhängigkeiten sind erlaubt.

### 🟡 In Bearbeitung

-   Erstellung eines detaillierten, überprüfbaren Designs (Editor/Backend-Auswahl + Veröffentlichungs-/Synchronisations-Workflow + Umsetzungsschritte).

### ⚠️ Blockierend/Offen

-   Genaue Bedeutung von "Echtzeitbearbeitung" (Echtzeitvorschau vs. automatisches Speichern vs. Mehrbenutzer-Kollaboration).
-   Bearbeitungsumfang (nur wenige Seiten/Einzeldateien oder der gesamte `content`-Bereich).
-   Veröffentlichungs-Workflow (nur als Entwurf speichern vs. Synchronisation mit `content/` zur Auslösung des Zola-Builds).

---

## Wichtige Dateien

-   `config.toml`: Site-Konfiguration und Navigation; Standard-Theme ist light, Mehrsprachigkeit wird unterstützt.
-   `templates/head.html`: Globale Einbindung von `static/css/reset.css`, `static/css/suCSS.css`, `static/css/style.css` und `static/js/script.js`, sowie CDN-Einbindung von `rxjs`, `highlight.js`, `mermaid`.
-   `static/css/suCSS.css`: Design-Tokens (CSS-Variablen) und Basis-Komponentenstile (Formulare, Buttons, Tabellen, etc.); enthält light/dark Theme-Variablen.
-   `static/css/style.css`: Benutzerdefinierte Site-Styles (Navigation, Footer, Slider, Shortcode-Styles, etc.).
-   `static/js/script.js`: Theme-Switching (`data-theme` + localStorage) und Slider-Interaktion; kann für Admin-Seiten wiederverwendet werden, um ein konsistentes Erlebnis zu gewährleisten.
-   `content/**`: Markdown-Inhaltsquellen; enthalten YAML Front Matter (`---`) und Shortcodes (z.B. `{{ slideshow(...) }}`).

---

## Schlüsselentscheidungen

| Entscheidung | Grund | Alternative | Datum |
|--------------|-------|-------------|-------|
| Standardempfehlung: "Reine JS-Admin-Seite + Supabase Speicher/Authentifizierung", Editor-Priorität für Vditor | Aktuelles Projekt hat keine Node/React-Build-Kette; bestehende CDN-Abhängigkeitskonvention; Vditor bietet WYSIWYG/IR/SV drei Modi und die Positionierung als "Browser-basierter Markdown-Editor", niedrigste Integrationskosten | React + shadcn + Milkdown/TipTap; oder GitHub Discussion/Issue als Backend | 2026-01-06 |

---

## Zusammenfassung der Ist-Analyse

### Technologie-Stack/Deployment

-   Site ist Zola (`config.toml`), GitHub Actions verwendet `shalzz/zola-deploy-action` für Deployment auf `gh-pages` (`.github/workflows/*.yaml`).
-   Site tendiert zu "minimalem JS": Aktuell ist das einzige Skript `static/js/script.js`, der Rest ist CDN (`rxjs`, `highlight.js`, `mermaid`).

### Theme und Designsprache (muss beibehalten werden)

-   Visuell basierend auf CSS-Variablen von `suCSS.css`: `--bg/--bg-light/--text/--text-light/--accent/--accent-light/--link/--border` etc.
-   Theme-Switching-Mechanismus: `document.documentElement[data-theme="light"|"dark"]`, synchronisiert mit `prefers-color-scheme` + localStorage (siehe `static/js/script.js`).
-   Komponentenstil: Buttons/Formulare/Tabellen/Listen-Marker etc. sind in `suCSS.css` definiert; neue UI-Elemente sollten diese semantischen Elemente und Variablen priorisiert wiederverwenden, anstatt ein neues Design-System einzuführen.

### Inhaltsmerkmale (beeinflusst den Editor)

-   Markdown kann enthalten:
    -   YAML Front Matter (Beispiel: `content/gcores-talks.md`)
    -   Zola Shortcodes (Beispiel: `{{ slideshow(...) }}`, Templates in `templates/shortcodes/*`)
    -   Hervorgehobene Code-Blöcke, mermaid Code-Blöcke
-   Risikopunkte: Ein WYSIWYG-Editor, der versucht, Shortcodes zu "verstehen/rendern", könnte den Originaltext umschreiben oder entfernen; es muss ein Editor/Modus gewählt werden, der "unbekannte Syntax unverändert lässt", oder im Editor ein "Quelltext-Modus" als Fallback bereitgestellt werden.

---

## Editor-Recherche (Online + Material)

> Ziel: Einbettbar, unterstützt WYSIWYG (Rich-Text/What-You-See-Is-What-You-Get) und kann Markdown ausgeben; idealerweise ohne Build-Kette (reines JS/CDN).

| Kandidat | Positionierung/Modi | Integrationsform | Theme/Anpassung | Wichtige Punkte | Fazit für dieses Projekt |
|---|---|---|---|---|---|
| **Vditor** | Browser-basierter Markdown-Editor; unterstützt WYSIWYG/IR(ähnlich Typora)/SV Split-Screen | Natives JS; offizielle Website und CDN-Form sind ausgereift (Seitentext betont explizit drei Modi) | Hat viele eigene Themes und Konfigurationsoptionen; kann über CSS-Variablen an suCSS angepasst werden | Unterstützt Echtzeitspeicherung, Einfügen/Drag&Drop-Upload etc. (siehe Beschreibung auf offizieller Website); MIT Lizenz (`https://unpkg.com/vditor@3.10.4/LICENSE`) | **Erste Empfehlung**: Passt am besten zu "Keine Build-Kette + Echtzeitbearbeitung"; muss dringend überprüft werden, ob "Shortcodes/Front Matter nicht zerstört werden" |
| **TOAST UI Editor** | Markdown/WYSIWYG Dual-Modus | CDN (`uicdn.toast.com/editor/latest/...`) oder npm | Hat Theme/Stylesystem; kann über benutzerdefiniertes CSS angepasst werden | Dokumentation unterstützt explizit CDN und das Deaktivieren von `usageStatistics` (`https://unpkg.com/@toast-ui/editor@3.2.2/README.md`) | Alternative: Ausgereift, aber Stylesystem ist "schwerer", Anpassung an suCSS-Theme könnte aufwändiger sein; Lizenz muss bei Integration noch geprüft werden |
| **Milkdown** | Plugin-basierter WYSIWYG Markdown-Editor (Typora-Stil), ProseMirror + remark | Benötigt normalerweise einen Bundler (React/Vue etc.) | Kann tiefgehend angepasst werden, aber Engineering-Kosten sind hoch | README betont seine WYSIWYG Markdown-Positionierung und MIT-Lizenz (`https://raw.githubusercontent.com/Milkdown/milkdown/main/README.md`) | Geeignet als Kandidat, wenn "React/shadcn eingeführt werden soll"; passt nicht zum aktuellen Zola-Projekt (benötigt Build-Kette) |
| **TipTap** | Rich-Text-Editor Toolkit (eher HTML/Schema-orientiert) | React/Vue/Svelte etc. | Starke Anpassungsmöglichkeiten, aber Markdown erfordert oft zusätzliche Konvertierung und Einschränkungen | Offizielle Website betont "real time collaboration" etc., aber Markdown-first ist nicht der Kern | Nicht als erste Wahl empfohlen (Markdown- und Shortcode-Szenarien sind komplex) |

---

## Backend-Recherche (Ohne manuelle Backend-Programmierung)

| Lösung | Benötigt eigenen Service? | Authentifizierung/Berechtigungen | Speicher/Historie | Beziehung zu Zola-Veröffentlichung | Fazit |
|---|---|---|---|---|---|
| **Supabase** | Nein (gehostete DB/Auth/Storage) | Frontend-Direktverbindung + RLS; anon key kann öffentlich sein | Zeilenbezogene Historie in DB muss selbst erstellt werden (kann revisions Tabelle sein) | Benötigt "Synchronisation mit repo/content", um die statische Site zu beeinflussen | **Empfohlene Standardlösung**: Umsetzung am direktesten; klare Sicherheitsgrenzen (RLS); Bildspeicherung auch einfach |
| **GitHub Issue/Discussion** | Theoretisch kein Service nötig, aber in der Praxis oft OAuth Token Exchange Service erforderlich | OAuth-Workflow komplex (reines Frontend kann Token-Austausch schwer sicher handhaben) | GitHub hat natürlicherweise Historie/Audit | Synchronisation mit der Site erfordert immer noch Skripte, um issue/discussion in `content/` umzuwandeln | Alternative: Wenn der Wunsch nach "Inhalte als GitHub-Daten" stark ist, kann in Betracht gezogen werden; aber Authentifizierung/Upload/Synchronisation ist komplexer |

Ergänzung: Der OAuth Web Flow von GitHub Docs erfordert im Schritt "Code gegen Access Token tauschen" explizit sowohl `client_id` als auch `client_secret`. Dies macht eine "reine Frontend-Direktverbindung zu GitHub als Schreib-Backend" aus Sicherheitssicht schwer realisierbar (es sei denn, ein Token Exchange Service wird eingeführt oder eingeschränkte Schemata wie device flow werden verwendet).

---

## Schnelle Übergabe

**Nächste Schritte hier fortsetzen:**

1.  In `plan.md` die Standardannahmen für "Echtzeitbearbeitung/Bearbeitungsumfang/Veröffentlichungs-Workflow" und die von Ihnen zu überprüfenden Entscheidungspunkte bestätigen.
2.  Basierend auf Ihrem Feedback, eine endgültige Empfehlung aus den drei Lösungsansätzen (Supabase / GitHub Discussions / React-Ansatz) treffen und die Umsetzungsschritte festlegen.

**Zu beachten:**

-   Jede UI (einschließlich Admin) muss die CSS-Variablen und den light/dark-Mechanismus von `suCSS.css` wiederverwenden, ohne neue, den Stil zerstörende Theme-Systeme einzuführen.
-   Muss dringend überprüft werden: Ob der Editor Zola-Shortcodes und Front Matter umschreibt.

---

*Letzte Aktualisierung: 2026-01-06 20:30 von Codex*