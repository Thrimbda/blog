# Test Report: Daily Log Split Implementation

## Result

PASS.

## Build

Command:

```sh
nix --extra-experimental-features 'nix-command flakes' shell nixpkgs#zola -c zola build
```

Result:

- PASS
- Created 258 pages and 9 sections.
- Internal anchor link check passed with 0 anchors checked.

## Content Migration

Generated by `scripts/split-daily-logs.mjs`:

- `diary-2020`: 61 daily pages
- `diary-2026`: 13 daily pages
- `gcores-talks`: 128 entry pages

The former aggregate sources now live under `scripts/daily-sources/` as migration input. Published Zola routes are daily pages and archive pages.

## Frontend Presentation

The final archive presentation is a continuous daily feed:

- `/gcores-talks/`
- `/diary/2020/`
- `/diary/2026/`

Each archive renders full entry content inline. Each entry title and `[打开这一天]` link opens the permanent daily page.

## Infinite Loading

PASS.

The archive keeps real Zola pagination links and enhances the `继续加载` link with JavaScript:

- Browser automation loaded `/gcores-talks/`.
- Initial entry count: 8.
- After enhanced load: 16.
- Live status: `已加载 8 则`.

The automatic observer is throttled by scroll position so image layout timing cannot trigger many pages at once.

## Mobile Date Access

PASS.

Each archive has a native date select plus `[打开]` link.

Verified:

- `/diary/2020/` can select `/diary/2020/2020-04-20/`.
- `/gcores-talks/` can select duplicate-date entry `/gcores-talks/2025-07-23-02/`.
- `/diary/2020/2020-04-20/` opens as a standalone daily page and links back to `/diary/2020/`.

## Viewport Metrics

Raw metrics: `docs/viewport-metrics.json`

| Viewport | Page | Entries on first page | Date options | Horizontal overflow |
|---|---|---:|---:|---|
| iphone17 | gcores | 8 | 128 | no |
| iphone17 | diary-2020 | 10 | 61 | no |
| iphone17 | diary-2026 | 6 | 13 | no |
| 13in | gcores | 8 | 128 | no |
| 13in | diary-2020 | 10 | 61 | no |
| 13in | diary-2026 | 6 | 13 | no |
| 27in | gcores | 8 | 128 | no |
| 27in | diary-2020 | 10 | 61 | no |
| 27in | diary-2026 | 6 | 13 | no |

Screenshots: `docs/screenshots/`

## Computer Use

PASS.

Inspected with Google Chrome:

- iPhone-class window: `/gcores-talks/`, date select and `[打开]` visible above the feed, full entries readable.
- 13-inch window: `/diary/2026/`, sticky left date tool plus right daily feed.
- 27-inch window: `/diary/2026/`, content remains centered/constrained and does not stretch.

## Notes

- The non-default language files are not present in this worktree; structural navigation links now fall back to default-language routes when translated content is missing.
- `data/daily-index.json` exists because Zola paginator templates do not expose the full section page list needed for complete date jump controls.
